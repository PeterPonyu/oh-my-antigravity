{
  "name": "antigravity",
  "version": "0.0.0-private",
  "private": true,
  "description": "Antigravity experimental beta: a clean-room local agent-harness scaffold.",
  "type": "module",
  "license": "MIT",
  "author": "Zeyu Fu",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PeterPonyu/oh-my-antigravity.git"
  },
  "bugs": {
    "url": "https://github.com/PeterPonyu/oh-my-antigravity/issues"
  },
  "homepage": "https://github.com/PeterPonyu/oh-my-antigravity#readme",
  "keywords": [
    "antigravity",
    "agent-harness",
    "clean-room",
    "experimental",
    "beta",
    "local-first"
  ],
  "bin": {
    "antigravity": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "start": "node ./src/cli.ts",
    "cli:help": "node ./src/cli.ts --help",
    "cli:status": "node ./src/cli.ts status",
    "cli:version": "node ./src/cli.ts --version",
    "build": "tsc -p tsconfig.build.json && node -e \"require('fs').chmodSync('dist/cli.js', 0o755)\"",
    "prepack": "npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "node ./scripts/verify.mjs --lint-only",
    "test": "node --test",
    "audit:negative": "node ./scripts/verify.mjs --audit-only",
    "smoke:pack": "node ./scripts/pack-smoke.mjs",
    "verify": "npm run typecheck && npm run lint && npm test && npm run cli:help && npm run cli:status && npm run cli:version && npm run audit:negative"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3"
  }
}
